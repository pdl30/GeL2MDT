upstream web {
    server web:8000;
}

server {

    listen 8000;
    server_name localhost;
    proxy_connect_timeout 300s;
    proxy_read_timeout 300s;
    location /static/ {
        autoindex on;
        alias /static/;
    }

    location / {
        proxy_pass http://web;
    }
}
